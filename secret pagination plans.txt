have a dict of paginated messages
paginated = {message id: ['type', 'last used time*', metadata]

metadata can be the current page number in e!bee or current category in e!about

*if its been 3 minutes since the last used time, delete the key from the dict

add an if to the on react

if payload.emoji in ('▶', '◀'):
    if last used not longer than 3 minutes ago:
        del pagesandstudd[payload.message.id]
    if ◀:
        call = metadata - 1 or something
    elif ▶:
        call = metadata + 1 or something
    call(type, metadata)